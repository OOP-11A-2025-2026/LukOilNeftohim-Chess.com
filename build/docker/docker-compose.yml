services:
  neftomate:
    image: maven:3.9.1-eclipse-temurin-17
    container_name: chess

    working_dir: /app

    command: mvn compile exec:java

    volumes:
      - ../..:/app
      - neftomate_m2:/root/.m2

    stdin_open: true
    tty: true

volumes:
  neftomate_m2:
